WITH BadIdeas AS (
	SELECT c.COLUMN_NAME
	FROM INFORMATION_SCHEMA.COLUMNS c
	GROUP BY c.COLUMN_NAME
	HAVING COUNT(DISTINCT c.DATA_TYPE) > 1)
SELECT cl.*
FROM BadIdeas bi
INNER JOIN INFORMATION_SCHEMA.COLUMNS cl ON cl.COLUMN_NAME = bi.COLUMN_NAME
ORDER BY bi.COLUMN_NAME, cl.DATA_TYPE;